/**
 * Decorators directory
 *
 * Place custom TypeScript/class decorators here as the application grows.
 *
 * Examples of decorators you might add:
 *   - @CacheResponse(ttl) — cache a controller method's response in Redis
 *   - @Roles('admin')     — declarative role guard for route handlers
 *   - @Validate(schema)   — Zod schema validation decorator
 *
 * Fastify uses preHandler hooks rather than decorators for route-level
 * concerns, but decorators are useful for service-layer cross-cutting logic.
 */
